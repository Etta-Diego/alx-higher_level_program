#!/usr/bin/python3
def update_dictionary(a_dictionary, key, value):
    update = a_dictionary[key]
    update = value
    return update
    #for keys in a_dictionary:
     #   print("{}: {}".format(keys, a_dictionary[keys]))
